# ODN Tools Pro — ODN 设计理念与开发规格

> 本文档是 ODN Tools Pro 后续 ODN 自动化设计功能的长期规则基准。
> 新增或修改 ODN 1.0 / 2.0 / 3.0 功能时，先阅读并遵守本文档；如用户后续明确修改规则，以最新讨论为准并同步更新本文档。

## 1. 当前工程阶段

当前“自动组网/规划”发生在设计初始阶段。

已知输入主要是：
- POLE 点位置
- Pole Edge 线网络
- 已经规划并放置的 FAT

此阶段尚未必有：
- FDT
- BB
- SFC CL
- 普通 CL
- 命名结果
- 最终光缆网络

因此不能假设这些对象已经存在，也不能用已有 ODN Cable 拓扑作为唯一输入来反推当前设计。

## 2. 基础物理网络原则

### 2.1 Pole Edge 是唯一允许的光缆物理路由

所有自动生成的光缆/预链接缆必须严格沿 Pole Edge 网络行走。
禁止为了追求距离短而直接用非 Pole Edge 的直线连接两个点。

### 2.2 FAT 与 Pole 的挂接

FAT 为了避免地图线条重合，可能故意从 Pole 坐标轻微偏移。
默认：FAT 距某 Pole ≤ 3m，则视为该 FAT 挂接在该 Pole 上。

FAT 挂杆距离是设计标准参数，ODN 2.0 默认 3m，但允许用户修改。

### 2.3 经过 FAT 不等于连接 FAT

一条光缆沿 Pole Edge 路由时，可以经过某个 FAT 所在的 Pole，但不代表必须把该 FAT 纳入当前 Link。

示例：
- 物理路线可以经过 FAT3 所在杆位；
- 逻辑 Link 可以是 FAT2 → FAT4；
- 只要物理路径全部沿 Pole Edge 即可。

## 3. ODN 节点定义

### FDT
- ODN 中的汇聚/分叉节点。
- 后续自动选址应优先考虑位于 Pole Edge 网络中自然汇聚、分支能力较强且整体省缆的位置。
- FDT 不以“覆盖 FAT 数最多”为唯一目标，应兼顾网络合理性和总缆长。

### FAT
- 当前初始组网阶段的主要已知接入节点。
- FAT 最终应归属某一个 FDT 的某一条 Link。
- 当前默认一个 FAT 最终只归属于一个 Link，不能在同一最终方案中被多个 Link 同时占用。

### BB
- 用于 FDT 某一 Link 内发生分叉、并造成回缆过长的情况。
- 当前核心规则：如果 Link 的实际 Pole Edge 路由形成回缆，回缆长度 > 100m，则需要考虑在适当的分叉节点设置 BB。
- BB 不是第一优先级解决手段；应先尝试重新组合 FAT Chain / Link，以减少或消除不必要回缆。

### SFC CL
- SFC CL 用于预链接缆过长的接续。
- 与普通 CL 明确区分。
- 当前规则：每条预链接缆最多使用 1 个 SFC CL。

### 普通 CL
- 主要处理 FEEDER 过长或分叉过多等问题。
- 当前阶段不作为核心自动优化对象；用户可以人工调整。

## 4. ODN 2.0 当前规则

ODN 2.0 必须参数化，而不是把规则硬编码为唯一方案。

### 4.1 设计标准界面

设计标准选择：
- ODN 2.0
- ODN 1.0（后续）
- ODN 3.0（后续）

接入节点类型可勾选：
- FAT
- BB
- SFC CL

因为部分方案可能只做 FDT → FAT，不使用 BB/SFC CL。

参数：
- 每个 FDT 最大 Link：默认 4，可修改
- 每条 Link 最大 FAT：默认 4，可修改
- 每条预链接缆最大长度：默认 455m，可修改
- FAT 挂杆最大距离：默认 3m，可修改

“每条预链接缆”的定义是：任意两个相邻 ODN 节点之间的一段，例如：
- FDT → FAT
- FAT → FAT
- FAT → BB
- BB → FAT
- FDT → SFC CL
- SFC CL → FAT

不是把整条 Link 的总长度定义为 455m。

## 5. 回缆定义（必须严格按 Pole Edge 几何）

“拐弯”不是回缆；“方向改变”不是回缆。

只有同一条 Link 的实际 Pole Edge 路由发生重复往返经过同一 Pole Edge 时，才产生回缆。

示例：

FDT-FAT1-FAT2-FAT3-FAT4

如果真实 Pole Edge 路由是：

Pole0 → Pole1 → Pole2 → Pole3 → Pole2 → Pole4

那么 Pole2 → Pole3 → Pole2 是回缆。

如果 Pole2-Pole3 = 50m，则：
- 去程 = 50m
- 回程 = 50m
- 回缆长度 = 100m

因此 ODN 2.0 默认回缆限制为 100m。

程序计算回缆时，必须从完整的实际 Pole Edge 路由序列计算，不能仅根据方向、角度或节点距离猜测。

## 6. FAT Chain / Link 的核心设计思想

### 6.1 先 Chain，后 FDT

正确顺序不是：

FDT → 找最近 16 个 FAT → 每 4 个一组。

正确顺序是：

FAT + POLE + Pole Edge
→ 发现合理 FAT Chain 候选
→ Chain 组合
→ 每个 FDT 最多选若干 Chain/Link
→ 根据 Chain 起点/网络位置反推 FDT 候选位置
→ 验证 BB / SFC CL / 长度等约束
→ 迭代优化。

### 6.2 Chain 是有序 FAT 序列

例如：

FAT1 → FAT3 → FAT5 → FAT7

可以是一个合法 Chain，即使物理路径途中经过 FAT2 所在杆位。

### 6.3 为什么允许跳过中间 FAT

目前明确有两类原因：

#### A. 为满足预链接缆最大长度而错开连接

例如在线性网络上，为使相邻 ODN 节点之间的预链接缆均符合最大长度，可以交错取 FAT。

示例：
- L1：FAT1 → FAT3 → FAT5 → FAT7
- L2：FAT2 → FAT4 → FAT6 → FAT8

前提是每一段实际 Pole Edge 路由均满足最大预链接缆长度。

#### B. 为避免回缆而主动错开分配

例如某条主路上出现侧向分支时，不一定把经过的所有 FAT 都塞入同一 Link；可以把不同区域的 FAT 重新分配到不同 Link，使路线减少往返。

### 6.4 跳过 FAT 的前提

无论因为什么跳过：
- 实际物理路线必须仍然沿 Pole Edge；
- 不允许用非 Pole Edge 直线“跨接”；
- 必须重新检查相邻 ODN 节点之间的预链接缆长度；
- 必须重新检查回缆；
- 最终 FAT 不能重复归属多个 Link。

## 7. Chain/Link 优化优先级（ODN 2.0）

建议按照以下层次评价候选方案：

1. **Pole Edge 合法性**：必须沿 Pole Edge。
2. **容量约束**：FDT Link 数 ≤ 参数；每 Link FAT 数 ≤ 参数。
3. **单段预链接缆长度**：任意相邻 ODN 节点之间 ≤ 设定最大值（默认455m）。
4. **回缆约束/回缆长度**：默认 ≤100m；优先通过重新组合 Link 消除或减少回缆。
5. **Link 区域连续性**：一条 Link 应形成一片连续、紧凑的网络区域；允许拐弯，不要求固定罗盘方向。
6. **BB 数量**：尽量少用。
7. **SFC CL 数量**：尽量少用；每条预链接缆最多1个。
8. **总缆长**：在上述工程合理性满足后，再尽量节省总缆长。

## 8. Link 区域而不是“方向分组”

不要把 Link 固定定义为“北/东/南/西四个方向”。

正确概念是：
- 从一个汇聚节点出发；
- 沿 Pole Edge 网络形成一个连续的可达区域；
- 该区域内可以拐弯，也可以经过其他 FAT 所在 Pole；
- 但逻辑上选择哪些 FAT 成为 Chain 节点，要依据网络连续性、长度约束和回缆情况决定。

因此“方向分组”不是 ODN 2.0 的正式术语；应使用“Link 区域/网络区域”。

## 9. FDT 规划思想

FDT 不应简单通过“覆盖 FAT 数量最大”来选址。

FDT 候选应综合考虑：
- 周边 Pole Edge 的自然汇聚/分支能力；
- 连接候选 Chain 起点的网络距离；
- 4 Link / Link FAT 容量；
- 单段预链接缆长度；
- 回缆；
- BB 数量；
- SFC CL 数量；
- 总体缆长；
- 区域合理性。

优先选择“合理 + 省缆”的位置。

FDT 候选规划应支持地图交互复核：
- 点击候选 FDT；
- 地图高亮 FDT 位置；
- 地图显示其覆盖 FAT；
- 可以单独点击 L1/L2/L3/L4，只显示当前 Link 的 FAT 和实际 Pole Edge 路由；
- 支持定位方案；
- 支持选中当前 Link/方案覆盖的 FAT；
- 人工调整后应能够锁定/确认方案，后续自动优化不得覆盖人工决定。

## 10. 人机协同

程序负责：
- 空间索引
- Pole Edge 网络分析
- 路径搜索
- Chain 候选发现
- 约束检查
- 方案评分
- BB / SFC CL 建议
- FDT 候选推荐

设计人员负责：
- 现场合理性判断
- 确认/调整 FDT
- 必要时调整 FAT 归属
- 确认 BB/SFC CL 是否工程合理

人工一旦确认/锁定：
- FDT 位置应保持；
- FAT 归属应保持；
- Link 结构应保持；
- 后续自动优化只能在未锁定部分进行。

## 11. SFC CL / 超长处理

如果某条预链接缆 >455m：
1. 优先尝试调整 Chain/Link 连接顺序；
2. 若允许 SFC CL，则尝试使用 1 个 SFC CL；
3. 若仍无法满足，且合理位置没有合适 Pole，可在合适位置新增 1 根 Pole，并更新 Pole Edge；
4. 若仍无法满足，则不能继续堆叠第二个 SFC CL，应回到 FDT/Chain/区域划分层重新优化。

例如约 900m 的线路，可以在合理位置新增 Pole，为 SFC CL 提供合理安装位置/网络挂接条件，只要最终每个相邻 ODN 节点之间的预链接缆符合限制。

若超长达到约 1000m 甚至更长，优先考虑重新调整 FDT 位置或 FAT 归属，而不是无限增加 Pole/SFC CL。

## 12. 暂时不做的事情

除非后续明确要求，否则当前不把以下内容作为 ODN 2.0 第一阶段核心自动化：
- 普通 CL 的自动规划；
- 完全自动且不可人工修改的 FDT 决策；
- 用纯空间最近邻替代 Pole Edge 网络拓扑；
- 用直线距离代替实际 Pole Edge 路由距离；
- 遇到 FAT 就强制把 FAT 加入 Link。

## 13. 开发约定

1. 新功能开发前先读取本文档。
2. 用户新提出的明确规则优先于旧版本实现，并立即更新本文档。
3. 不要为了“让结果看起来有数字”而输出没有工程意义的长度指标。
4. 如果某个指标尚不是最终定义，界面必须明确标注“候选/代价”，不能冒充最终 Link 长度。
5. 每次重大规则变更都应留下清晰的 Git commit message。
6. ODN 1.0 / 2.0 / 3.0 应逐步做成不同的规则配置/引擎，而不是复制整套代码。
