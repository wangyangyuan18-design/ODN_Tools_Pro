# ODN 线路与偏移设计规则

> 这是 Link Design / Distribution Cable 偏移算法的长期规则记录。后续讨论和代码修改以本文件为准；已经确认的规则不要因为后续调试而遗忘或重新解释。

## 0. 图纸阅读与问题编号

- 统一把图纸视为 **从上往下看**。
- 图片中的蓝色小点 = **Pole Edge 节点/电杆节点**。
- 黄色、绿色等颜色只是为了对照某一条 Link/Cable，不代表图层类型；除非明确说明，不把黄色线理解为 Pole Edge。
- 后续问题按图片编号讨论：1、2、3……；同一问题中的线路用“黄色1、绿色1、绿色2”等称呼，便于对照。

## 1. 已确认：普通 Pole 节点不得被两条独立光缆同时占用

### 1.1 普通规则
- 一个普通 Pole Edge 节点，正常情况下只允许 **1 条独立 Cable** 通过/连接。
- 如果另一条 Cable 与已占用的普通 Pole 节点发生共点，必须重新选择 Lane/过渡位置，不能让两条独立 Cable 都落到同一个杆点。
- 这里的“不能共点”针对普通路线节点，不是禁止两根光缆在工程设备节点进出。

### 1.2 允许多缆共点的工程节点
以下属于明确允许多缆进出的特殊节点：
- FDT：作为出缆/进缆节点；
- FAT Return：FAT 回缆形成的同一点进出缆；
- BB；
- SFC / CL Closure。
后续如增加新的工程节点类型，应明确列入此白名单，不通过模糊几何条件自动放宽普通 Pole 节点限制。

## 2. 已确认：相邻并行光缆 Lane 间距
- 普通相邻 Cable Lane 的物理间距 = **0.5 m**。
- 0.5 m 是普通并行路线的核心偏移距离。
- 不能为了处理普通转角而把 0.5 m 改成 0.3 m。

## 3. 已确认：0.3 m 只用于同一点特殊分叉/回流
- **0.3 m 不是普通 Pole Edge 转角参数。**
- 普通 Lane 转换、普通直角/折角，不使用 0.3 m control distance。
- 0.3 m 只保留给同一点多缆分叉/进出缆控制，例如 FDT、BB、SFC/CL、FAT Return 等特殊节点。

## 4. 已确认：Route 选择不是简单最短路
路线优先级应综合：
1. 连续方向上的实际经过长度；
2. 尽量减少不必要的拐弯；
3. 尽量减少回头/反向（尤其明显的 135°+ reversal）；
4. 尽量减少对其他线路和已有 Lane 的干扰；
5. 在满足工程约束的情况下，再比较总路线长度。
因此，某条路线即使总长度略短，如果需要明显回转、穿插或破坏其他主线的连续性，也不应简单因为“最短”而被选择。

## 5. 已确认：主线与 Lane 要按完整 Route 判断
- Link 编号（L1/L2/…）是业务标识，不等于几何 Lane 编号。
- 几何处理应考虑完整 Route 在多个 Pole Edge 上的连续性。
- 应尽量让一条 Route 在连续道路段上保持稳定 Lane，不应在每一个 Pole Edge 上重新独立决定左右偏移。

## 6. 已确认：普通转角必须连续
- Cable 应沿原始 Pole Edge Route 做连续偏移。
- 同一 Lane 穿过 Pole 节点转角时，不能回到原始 Pole 节点再重新起步，也不能人为插入 0.3 m 普通控制段。
- 转角的基本关系仍由 0.5 m Lane offset 和几何方向确定。
- 如果尖角导致 miter 不合理，可采用不回穿原始节点的 bevel 等连续几何处理。

## 7. 已确认：相对位置连续（问题2）
### 7.1 基本原则
- 光缆在连续共同路由上，应尽量保持与其他光缆的**相对位置**，而不是固定某个绝对 XY 方向。
- 例如从左往右时，绿色1开始位于黄色1下方 0.5 m；路线一起向上转弯后，绿色1应继续保持在黄色1的相对右侧 0.5 m；再一起向左转后，绿色1自然变成黄色1上方 0.5 m。
- 因此 Lane index 表示的是相对于当前 Route 方向的几何位置；不能因为道路方向改变就重新把 Cable 随意换到另一侧。

### 7.2 什么时候允许改变相对位置
只有在存在明确工程/几何原因时才允许 Lane Change，例如：
- 某一段路线停止、结束或与其他路线分离；
- 两条路线的后续拐弯方向不同；
- 当前 Lane 发生真实冲突，必须让位；
- 根据各自完整 Route 的目的方向，必须重新选择道路的另一侧；
- 新 Cable 加入现有共同路由，需要在既有 Cable Group 的外侧增加 Lane。

不允许仅仅因为“当前 Edge 上另一个 slot 空着”就改变一条已经稳定的 Cable 的相对位置。

### 7.3 Lane 编号示例
以左右方向道路为例：
- 主线占 0；
- 上方为 +1、+2、+3……；
- 下方为 -1、-2、-3……；
- 相邻 Lane 间距 0.5 m。

如果绿2已经占据 0 主线位置，黄1和绿1后来需要共同经过这段已经被占用的路线，则根据后续路线方向选择一侧：
- 后续需要走上方：黄1、绿1依次占 +2、+1；
- 后续需要走下方：依次占 -1、-2。
核心不是固定编号，而是保持三条线之间的相对顺序。

### 7.4 新 Cable 加入共同路线：不得插入既有 Cable Group 中间
当绿2加入黄1、绿1已经共同走的路线时：
- 如果绿2从现有 Group 上方加入，则加入到 Group 的最外侧；
- 如果从下方加入，则加入到 Group 的最外侧；
- 不因为 Lane 编号有空位就插入黄1与绿1之间。

也就是说，连续共同路由应被视为一个**Cable Group**，新增 Cable 从进入侧向 Group 外缘扩展。

### 7.5 问题2代码修改
已修改 `cable_offset_layout_v9.py`：
- 相邻 Edge 分配时优先继承上一 Edge 的 `slot`；
- 若原 Lane 被真实占用，优先向同一侧的更外层 Lane 移动；
- 只有原侧无法满足时才考虑跨到另一侧；
- 新 Cable 首先按其进入侧向外寻找 Lane，避免插入既有 Group 内部；
- 保留原有全局 Route priority；
- 保留普通 Pole node exclusivity；
- 保留 0.5 m spacing 和 0.3 m 特殊节点规则。

代码提交：`019074783dd9dd12de6f38a8fdff2b60f764ed54`。

## 8. 后续规则记录方式
每讨论一个问题，先记录：
- 图纸现象；
- 具体线路名称；
- 当前代码如何处理；
- 为什么产生当前结果；
- 最终达成的一致工程规则；
- 对应修改的代码模块/函数；
- 测试结果。

只有在“工程规则”层面确认后再修改代码，避免反复改参数。
