name: Temp Offset Lines 2
on:
  push:
    branches: [main]
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show suspect lines
        run: nl -ba cable_offset_core.py | sed -n '955,985p'
